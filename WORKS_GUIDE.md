# 作品栏目与素材替换

当前保留 11 个栏目，共 106 个素材位，已全部放入真实作品。其中包含 18 张方图创意、12 张竖版海报、6 张营销主图、3 张直播间设计图片、3 张局部内容优化图片、3 张详情长图、4 张 mind 角色作品、4 张展会效果图、21 张产品手册页面（肌醇 9 张、宝嘉力 12 张）、4 张 Logo 设计、2 张包装设计、4 张 China GT 项目赛车设计、4 个 AI 视频、9 张三维场景设计、6 个三维动态/动画作品与 3 张首页长图。

| 栏目 | 预留数量 | 位置前缀 | 展示方式 |
| --- | --- | --- | --- |
| 创意设计 · 方图 | 18（已填） | `creative-square` | 电脑与手机均三列普通网格，支持跳过和点击放大 |
| 创意设计 · 竖版 | 12（已填） | `creative-long` | 电脑与手机均三列普通网格，支持跳过和点击放大 |
| 营销主图设计 | 6（已填） | `marketing-main` | 位于直播间设计上方，电脑与手机均三列正方形卡片，支持跳过和点击放大 |
| 直播间设计 | 3（已填，含 1 张 GIF） | `livestream-design` | 位于营销主图下方；电脑与手机均三张一排，9:16 完整展示，保留 GIF 动画与点击放大 |
| 局部内容优化 | 3（已填） | `content-optimization` | 位于直播间设计下方、详情设计上方；电脑与手机均三列等高完整展示，支持跳过和点击放大 |
| 详情设计 | 3（已填） | `product-detail` | 位于局部内容优化下方、首页设计上方；电脑与手机均三列 9:20 预览，按住图片上下拖动阅读 |
| 首页设计 | 3（已填） | `homepage` | 位于详情设计下方、IP 上方；电脑与手机均三列 9:16 预览，按住图片上下拖动阅读 |
| IP 设计 | 4（已填） | `ip-design` | 持续缓慢环形转动，悬停角色暂停，支持拖动与左右箭头，无大图入口 |
| 展会设计 | 4（已填） | `exhibition` | 四张横幅效果图按原比例完整展示，电脑与手机均两列，可点击放大 |
| 线下物料设计 · 产品手册设计 · 肌醇 | 9（已填） | `product-manual` | 位于展会设计后；按原页码排列，大小图交错拼版，无图片下方名称，支持点击放大 |
| 线下物料设计 · 产品手册设计 · 宝嘉力 | 12（已填） | `sunseasons-manual` | 接在肌醇手册后，独立错落拼版与大图预览；无图片下方名称 |
| 线下物料设计 · Logo设计 | 4（已填） | `logo-design` | 接在产品手册后，按美之莉与薇达分组、每组两张并排，无图片下方名称，支持点击放大 |
| 线下物料设计 · 包装设计 | 2（已填） | `packaging-design` | 上半部线稿初始显示，点击横向按钮后自动过渡至下半部 3D 效果图 |
| 线下物料设计 · China GT项目赛车设计 | 4（已填） | `china-gt-racing` | 四张横版赛车涂装图按两列展示，无逐图名称，支持悬停与点击放大 |
| 三维设计 · 三维场景设计 | 9（已填） | `three-d-scene` | 三条等高列错位排版，组顶与组底齐平，无图片下方名称，支持点击放大 |
| 三维设计 · 三维动态/动画设计 | 6（已填） | `three-d-motion` | 统一 16:9 对齐网格；5 个 MP4 预览居中裁切，点开立即播放；H-L70S GIF 完整显示；核心部件动画封面取自 00:04 |
| AI 视频创作 | 4（已填） | `ai-video` | 统一 16:9 裁切预览；PC 与手机均为两列 2 × 2 网格，点击后直接播放完整视频 |

所有栏目都有单独锚点，可以通过作品目录直接跳转。除 IP 外，作品使用普通网格，不固定展台；首页与详情的单张预览框支持鼠标按住后上下拖动阅读完整图片，触屏支持手指上下滑动。这两栏取消「跳过本组」，原位置改为「点击图片拖动查看」黑底白字提示及上下箭头，点击不跳转。其他栏目的「跳过本组」仍前往下一组，最后一个栏目跳到设计能力。首页与详情的图片本身不触发弹窗，每张图下方新增独立的「点击查看大图」线框按钮，点击按钮可打开对应完整长图。没有真实素材时只保留预留布局和跳过入口。IP 保留自动慢转、角色悬停暂停、拖动和可点击箭头，不增加跳过按钮。图库大图使用精简查看器，顶部仅保留小号名称与关闭，底部为放大镜缩放滑块。

入场动效集中在首屏开场和分类标题。作品卡片本身不依次揭幕、不缩放、不加视差，继续按原有方式随下滑整组移动；所有图片保持完整比例。标题动效参数在 `src/motion/engine.js`，不会接管作品组原生滚动或 IP 转动。

## 营销主图设计

位于创意设计之后、IP 设计之前，栏目锚点为 `#marketing-main`，展台锚点为 `#marketing-main-sequence`。六张原图按提供顺序复制到 `public/works/marketing-main/`，不裁切、不压缩、不修改来源文件。比例均为 `1:1`，复用方图创意的 `MediaCard`、`ScrollSequence` 和大图查看器。分类目录与跳过顺序跟随栏目数据自动更新。

| 位置 ID | 原文件 | 网站文件 | 原始尺寸 |
| --- | --- | --- | --- |
| `marketing-main-01` | 5dfce6d231e6032d38b98a1094756d243aa667ed6468c-f7HP2i_fw1200.jpg | 01-fish-oil-tmall.jpg | 800 × 800 |
| `marketing-main-02` | 2223(1).jpg | 02-fish-oil-year-end.jpg | 750 × 750 |
| `marketing-main-03` | 红皇后.jpg | 03-ferrolip-forte.jpg | 800 × 800 |
| `marketing-main-04` | 画板 1 拷贝 3.jpg | 04-mini-fish-oil-jd.jpg | 800 × 800 |
| `marketing-main-05` | 517w主图---京东.jpg | 05-made-heart-5l.jpg | 800 × 800 |
| `marketing-main-06` | h1-m.jpg | 06-made-heart-7l.jpg | 800 × 800 |

## 放入真实素材

1. 文件放入 `public/works/`。
2. 打开 `src/work-media.js`，找到位置 ID。
3. 填入路径、作品名与图片说明。

例如，替换第一张创意方图：

```js
'creative-square-01': {
  src: '/works/creative-01.webp',
  poster: '',
  title: '你的作品名称',
  alt: '画面内容的简短说明',
},
```

视频示例：

```js
'ai-video-01': {
  src: '/works/ai-film-01.mp4',
  poster: '/works/ai-film-01-cover.webp',
  title: '你的 AI 短片名称',
  alt: '视频封面说明',
},
```

视频建议使用 H.264 MP4。封面 `poster` 可不填；未填写时尝试显示视频首帧。空 `src` 保留空位，填写后自动显示作品。普通图片和视频启用媒体预览；首页与详情长图在卡片内拖动阅读，IP 角色只在展台展示。

## 方图作品流

18 张作品均按原样复制到 `public/works/creative-square/`，对应 `creative-square-01` 至 `creative-square-18`。前 15 个编号与最初提供的文件一致；新增三张来自桌面「新增作品」文件夹，按提供顺序接在末尾。图片均为正方形，保留原始尺寸，没有裁切或重新压缩。

展台锚点为 `#creative-square`，优先展示以下 6 张，然后继续展示其余 12 张：

| 主展顺序 | 位置 ID | 作品名称 | 原文件 |
| --- | --- | --- | --- |
| 1 | `creative-square-01` | 双子星 · 篮球主题 | 双子星utral.jpg |
| 2 | `creative-square-04` | 金凯撒 · 极寒鱼油 | 二折页-正--3(1)(1)-拷贝-拷贝.jpg |
| 3 | `creative-square-15` | 意之宝 · 年货节 | jimeng-2026-01-27-5740…jpg |
| 4 | `creative-square-02` | 红皇后 · 云朵铁滴剂 | 铁滴剂.png |
| 5 | `creative-square-12` | 舒伯特 · 金护卫 | 舒伯特-(2).jpg |
| 6 | `creative-square-06` | 宝嘉力 · 3·8 焕新周 | 会员230000.jpg |

更改优先展出顺序只需编辑 `src/gallery-data.js` 中方图行的 `featuredIds`，不必重复素材或改组件。其余真实作品自动接在这些作品后面。

新增作品对应关系：`creative-square-16` 为 mini鱼油.jpg（16-mini-capsule.jpg，1254 × 1254）；`creative-square-17` 为宝嘉力推广.jpg（17-calcium-maternity.jpg，800 × 800）；`creative-square-18` 为作品集.jpg（18-iron-maternity.jpg，1000 × 1000）。原有 15 张的展示顺序不变，新增三张依次排在最后。

电脑三列、手机两列，同时展示多张作品，随原生页面下滑连续浏览全部 18 张，最后一排完整呈现后才进入下一组；无强制等待、无自动轮播。顶部显示标题、张数与「跳过本组」，可随时退出到竖版创意。直接点击图片查看大图，关闭或按 Esc 回到原位置。不再设置独立图库入口，也不显示重复英文标签或底部操作说明。

## 竖版作品流

12 张原图保存在 `public/works/creative-portrait/`，编号与提供时的顺序一致。栏目名称为「竖版创意」，沿用 `#creative-long` 锚点，编号为 `creative-long-01` 至 `creative-long-12`。

展示顺序由该行的 `featuredIds` 控制，当前素材编号依次为 9、8、4、12、2、3、5、6、7、10、11、1。「金凯撒 · 赛车体验」与「金凯撒 · 雪山赛车」互换后，分别位于第 4 个和第 12 个展示位，其余位置不变；素材编号及原文件保持不变。

| 位置 ID | 原文件 | 网站文件（creative-portrait/ 下） |
| --- | --- | --- |
| `creative-long-01` | lQLPKH5JHc-mfs3NBHXNA5OwLXcm8nVhX4UJmr6pEvk6AA_915_1141.png | 01-racing-mountain.png |
| `creative-long-02` | 儿童液体钙.jpg | 02-calcium-child.jpg |
| `creative-long-03` | 钙800.jpg | 03-calcium-active.jpg |
| `creative-long-04` | 舒伯特长图.jpg | 04-schubert-active.jpg |
| `creative-long-05` | 未标题-1.jpg | 05-beauty-fish-oil.jpg |
| `creative-long-06` | 鱼1.jpg | 06-fish-oil-gold.jpg |
| `creative-long-07` | 鱼2.jpg | 07-fish-oil-crown.jpg |
| `creative-long-08` | Gemini_Generated_Image_fnzerufnzerufnze-(1).png | 08-football-character.png |
| `creative-long-09` | 未标题-2.jpg | 09-racing-cream.jpg |
| `creative-long-10` | 桌面 / 3-4 拷贝 2.jpg | 10-fish-oil-press.jpg |
| `creative-long-11` | 桌面 / 血管.jpg | 11-iron-vessels.jpg |
| `creative-long-12` | 桌面 / 海报.jpg | 12-racing-experience.jpg |

与方图共用顺序展台，随下滑展示全部 12 张，不再只放 3 张精选。图片以多列网格连续移动，最后一排后才进入原有 IP 栏目，也可直接「跳过本组」到 IP。PC 三列、手机两列，展示顺序见上文。直接点击作品查看大图，关闭后回到原位置。

竖版区域当前试用统一 3:4 展示外框：图片按原比例完整居中，不裁切、不拉伸，比例差异以网页浅紫灰背景留白。仅缩略展示外框改变，大图、原文件、作品顺序与滑动方式不变。如需恢复原有高低错落排版，删除 `src/gallery-data.js` 竖版行的 `frameRatio: '3:4'` 即可，其他栏目不受影响。

这些作品是竖版海报，数据使用 `kind: 'image'`，不是 `long` 超长图阅读模式。默认比例为 3:4；第 1 张为 915:1141，第 2、3 张为 750:1052，第 12 张为 4724:7087，实际比例填写在 `src/work-media.js` 的 `ratio`。展台与大图默认完整适应，不裁掉标题、产品或底部说明；页面滚动带动整组网格连续上下移动，而不是单张切换。原文件按字节复制，未压缩、裁切或覆盖桌面及 D 盘文件。

## mind IP 展示

四张原图已按原样复制到作品目录，保留透明通道，未改动 D 盘源文件：

| 位置 | 原图 | 网站文件 |
| --- | --- | --- |
| `ip-design-01` | 大笑.png | `public/works/mind-laugh.png` |
| `ip-design-02` | 魔法.png | `public/works/mind-magic.png` |
| `ip-design-03` | 点赞.png | `public/works/mind-like.png` |
| `ip-design-04` | 打招呼.png | `public/works/mind-hello.png` |

栏目位于创意设计之后，锚点是 `#ip-design`。`src/gallery-data.js` 的 `characterName` 为背景字 mind，`characterLabel` 为展台顶部的系列名。每个姿态的标题、说明及浅色背景可在 `src/work-media.js` 中修改：

```js
'ip-design-01': {
  src: '/works/mind-laugh.png',
  poster: '',
  title: 'mind · 大笑',
  alt: '角色画面描述',
  description: '这个姿态的设计说明',
  background: '#e8e7da',
},
```

角色采用完整适应显示，不裁掉帽子、手势或脚部。新素材建议同样使用透明底 PNG / WebP，并尽量保持画面留边一致。不要把带实景背景的图片直接混入角色队列；完整场景适合独立陈列。

持续缓慢环形转动，4 个角色姿态约 22 秒一圈，进入视野后即开始，没有几秒一停的等待。鼠标碰到角色图像时停在当前位置，离开图像后继续；展台空白区域不暂停。鼠标拖动 / 手机横滑直接控制转动角度，左右箭头与方向键可转到相邻姿态。点击角色不放大，滚轮不参与交互，也没有独立暂停按钮。键盘聚焦、离开视野或切到后台时暂停；系统减少动态效果时只保留手动操作。连续速度位于 `src/ip-utils.js` 的 `advanceIPPhase`（当前每个姿态间距 5500ms），控制逻辑位于 `src/useIPOrbit.js`。

## 展会设计

已接入桌面的 UGA Vitafoods Asia 与阳光四季 Vitafoods 展台效果图，按提供顺序展示：

| 位置 ID | 原文件 | 网站文件（exhibition/ 下） | 原始尺寸 |
| --- | --- | --- | --- |
| `exhibition-01` | UGA_Vitafoods-Asia_Visual-1.jpg | 01-uga-vitafoods-asia.jpg | 3508 × 2058 |
| `exhibition-02` | UGA_Vitafoods-Asia_Visual-2.jpg | 02-uga-vitafoods-asia.jpg | 4961 × 2910 |
| `exhibition-03` | vitafoods-阳光四季设计稿-黑马8.jpg | 03-sunseasons-vitafoods.jpg | 12500 × 6942 |
| `exhibition-04` | vitafoods-阳光四季设计稿-黑马28.jpg | 04-sunseasons-vitafoods.jpg | 12500 × 6942 |

原文件按字节复制到 `public/works/exhibition/`，未裁切、压缩或修改桌面原图。栏目锚点为 `#exhibition`，作品展台为 `#exhibition-sequence`。电脑两列、手机单列，保留原比例和点击大图，沿用原生下滑与「跳过本组」。第 3、4 位为新加入的阳光四季作品，原有 UGA 两张的位置不变。

展会大图默认适应窗口完整显示，与其他图片查看器统一使用底部缩放滑块，范围从适应尺寸的 50% 至图片原始尺寸，不再提供「原尺寸」切换按钮。滑块最右端显示「原尺寸」，放大不会超过原图像素大小。放大后可拖动或触摸平移查看边角，关闭仍返回原位置。

## 线下物料设计 / 产品手册设计 / Logo设计 / 包装设计 / China GT项目赛车设计

主栏目锚点为 `#offline-materials`，位于展会设计之后、三维设计之前。主标题为「线下物料设计」，下设「产品手册设计」（`#product-manual-sequence`）、「Logo设计」（`#logo-design-sequence`）、「包装设计」（`#packaging-design-sequence`）和「China GT项目赛车设计」（`#china-gt-racing-design-sequence`）四个分类。主栏目使用 `rows`；产品手册分类内用 `collections` 依次放置「肌醇产品手册」（`#inositol-manual`）和「宝嘉力产品手册」（`#sunseasons-manual`），每册单独展示名称、拼版与大图预览，不跨手册翻页。Logo设计沿用同一层级结构，内设「美之莉 Logo设计」（`#meizhili-logo`）和「薇达 Logo设计」（`#widar-logo`）两个品牌组；电脑和手机均保持每组两张并排，隐藏逐图名称，查看器保留作品名称且不跨品牌翻页。包装设计使用两列交互卡片（手机为单列），每张原图的上半部线稿作为初始画面，点击横向按钮后自动播放 ShapeBlur 扫光与模糊渐现，切换到同一原图的下半部 3D 效果图；系统减少动态效果时直接切换。China GT项目赛车设计以两列横版卡片展示 4 张原图，不显示逐图名称，沿用统一的 PC 悬停放大与大图查看器。四个子分类标题都不显示跳过按钮；唯一的「跳过本组」位于线下物料设计主标题排版区，与营销主图设计采用相同标题布局，按钮前往三维设计。

美之莉 Logo 原文件位于桌面 `新增作品/deliverables-v3/美之莉 MEIZHILI/`，网站副本保存在 `public/works/logo-design/`。两张均为 4096 × 2304，按字节复制，不裁切、不压缩：

| 位置 ID | 原文件 | 网站文件 |
| --- | --- | --- |
| `logo-design-01` | `download-(100).jpg` | `01-meizhili-logo-construction.jpg` |
| `logo-design-02` | `download (99).png` | `02-meizhili-logo-application.png` |

薇达 Logo 原文件位于桌面 `新增作品/deliverables-v3/薇达 WIDAR/`，网站副本保存在同一 `public/works/logo-design/` 目录。两张同样按字节复制，不裁切、不压缩：

| 位置 ID | 原文件 | 网站文件 | 原始尺寸 |
| --- | --- | --- | --- |
| `logo-design-03` | `download---2026-09-11T182336.jpg` | `03-widar-logo-construction.jpg` | 4096 × 2304 |
| `logo-design-04` | `04-空间与纸品应用.png` | `04-widar-logo-application.png` | 1672 × 941 |

包装设计原文件来自桌面，网站副本保存在 `public/works/packaging-design/`。两张原图都由上下两幅组成，页面仅通过遮罩分别显示上半部和下半部，不生成新的裁切文件。当前已改用桌面根目录中的 `22.jpg` 与 `11.jpg`；分界按画面实际横向接缝设置为 4012px 与 4000px。卡片画幅固定采用每张图较短半幅的高度，点击前后尺寸不发生变化：

| 位置 ID | 原文件 | 网站文件 | 原始尺寸 |
| --- | --- | --- | --- |
| `packaging-design-01` | `C:/Users/TAO/Desktop/22.jpg` | `01-meizhili-blue-green-haircare.jpg` | 5729 × 8023 |
| `packaging-design-02` | `C:/Users/TAO/Desktop/11.jpg` | `02-meizhili-green-gold-haircare.jpg` | 5729 × 8018 |

交互组件为 `src/components/PackagingReveal.jsx`，样式在 `src/components/packaging-reveal.css`。`ShapeBlur.jsx` 由用户提供的 React Bits 组件改造，保留 Three.js 着色器形状模糊效果，但将鼠标跟随改为点击后自动扫过；图片本身同时从模糊、轻微缩放状态渐现。初始按钮文案为「点击渲染3d效果图」，切换后可返回包装线稿图。两个按钮首次进入视野时各播放一次放大、回弹和归位动画，点击切换不重播。点击卡片画面会打开全站共用的大图预览窗口，并可在两张包装作品之间连续浏览；切换按钮仍只负责线稿与 3D 效果转换。包装卡片同时加入全站统一的 PC 悬停规则，鼠标进入整卡放大至 1.04 倍，离开后在 0.32 秒内复原；手机端和系统减少动态效果时保持静止。

China GT 项目赛车设计原文件位于 `D:/作品集内容/作品集到7月了/`，网站副本保存在 `public/works/china-gt-racing/`。四张均为 5504 × 3072 JPG，按提供顺序逐字节复制，不裁切、不压缩：

| 位置 ID | 原文件 | 网站文件 |
| --- | --- | --- |
| `china-gt-racing-01` | `赛车2.jpg` | `01-racing-rear-view.jpg` |
| `china-gt-racing-02` | `赛车3.jpg` | `02-racing-multi-view.jpg` |
| `china-gt-racing-03` | `赛车42.jpg` | `03-racing-left-front-view.jpg` |
| `china-gt-racing-04` | `赛车92.jpg` | `04-racing-right-front-view.jpg` |

当前展示肌醇手册 9 张页面，按文件页码排序：**1、2、3、5、6、11、12、25、28**。根据用户选择移除了三个 PART 章节页（原页码 4、7、9）及人物品牌介绍页（原页码 8），通过 `gallery-data.js` 的排除列表同时移出拼版和查看器，不保留空位；原文件及 `work-media.js` 配置均保留，移除排除项即可恢复。未提供的页码不生成占位。剪贴板目录图片与 `肌醇手册-3.png` 的 SHA-256 完全相同，只展示一份；用户原文件均未删除或修改。

原文件为桌面的 `肌醇手册-{页码}.png`；网站路径为 `public/works/product-manual/inositol-{两位页码}.png`，素材编号为 `product-manual-{两位页码}`。全部是 6000 × 3375 PNG，按字节复制并核对 SHA-256，没有裁剪、重新压缩或改变图片内容。

电脑与手机均使用三列基础的大小图拼版（`gridLayout: 'mosaic'`）：封面横跨两列两行，右侧叠放主题介绍与目录；中间并排问题引入、信息解析、肌醇介绍；底部封底（原页码 28，当前第 8 张）作为右侧大图，左侧叠放成分解析与荣誉展示（原页码 25，当前第 9 张）。封底与荣誉展示通过 `featuredIds` 互换位置，实际展示及查看器顺序为 **1、2、3、5、6、11、12、28、25**，其余页面不变。九张填满五行基础网格。横向间距为 8–20px，纵向间距按 9/16 等比设置，让大小图均保持完整 16:9 比例且边缘对齐，不裁剪、不拉伸、不加外框。

两册分别设置 `hideCaptions: true`，不渲染图片下方名称，其他分类不受影响。仍保留图片替代文字、按钮无障碍名称及查看器内的页面名称。点击任意页面打开共享查看器，支持缩放至原尺寸，默认适应窗口，关闭后返回原位置与焦点。拼版没有内层滚动或固定展台；后续补充缺页时，在 `work-media.js` 对应原页码位置填入素材，自动按页码插入并套用 12 张循环拼版。

宝嘉力产品手册收录原页码 **1、2、3、5、6、7、8、9、13、14、16、25**，共 12 张，封面为第一张、封底为最后一张。来源为桌面 `新建文件夹 (4)/宝嘉力品牌手册-合-{页码}.png`，网站文件为 `public/works/sunseasons-manual/sunseasons-{两位页码}.png`，素材编号为 `sunseasons-manual-{两位页码}`。全部按字节复制并校验 SHA-256，原图宽 8054px，高 4530px（第 9 页高 4533px）；逐张保留真实宽高比例，不裁切、不拉伸、不重新压缩。产品简介与数据图表通过 `featuredIds` 互换，实际展示及查看器顺序为 **1、2、3、5、6、7、9、8、13、14、16、25**。12 张使用完整六行基础拼版，封面与产品简介（原页码 8）为大图，数据图表位于大图左侧上方，电脑和手机保持相同视觉顺序。两册之间留出 32–64px 间距。

## 直播间设计

当前展示顺序为：金凯撒 · 运动直播间 → 宝嘉力 · 马年直播间 → 宝嘉力 · 春季直播间。通过该栏目的 `featuredIds` 调整为 `03、02、01`，原始文件名与素材编号不变，电脑、手机及大图浏览顺序同步。

位于营销主图设计下方、局部内容优化上方，锚点为 `#livestream-design`。原文件位于 `D:/作品集内容/作品集到7月了/`，按提供顺序逐字节复制到 `public/works/livestream-design/`，已核对 SHA-256，不裁切、不转码、不重新压缩：

| 网站文件 | 原文件 | 原始尺寸 |
| --- | --- | --- |
| `01-calcium-spring.jpg` | jimeng-2026-02-13-9442-现代极简淡绿色系直播间，3D-高质感商业渲染效果图，8K-超高清，电影级光影，正...jpg | 1080 × 1920 |
| `02-calcium-new-year.jpg` | 直播.jpg | 1080 × 1920 |
| `03-fish-oil-sports.gif` | 651.gif | 900 × 1600，45 帧 |

三张均为 9:16，使用 `equalImageHeight: true` 和原图比例，电脑、手机均三张等高直接并排，不增加统一外框或内部留白。复用营销主图的 `MediaCard`、`ProjectGrid` 及共享查看器；图片可点击放大，缩放最多为原尺寸，关闭后返回原位置，电脑悬停放大效果不变。「跳过本组」前往局部内容优化。GIF 仍作为原生 `img` 显示，卡片与查看器均使用完整 GIF 文件，保留原有动画，不替换成静态封面或视频。

## 局部内容优化

位于直播间设计下方、详情设计上方，锚点为 `#content-optimization`。按提供顺序导入三张原图，源文件均来自 `D:/作品集内容/作品集到7月了/`：

| 网站文件 | 原文件 | 原始尺寸 |
| --- | --- | --- |
| `01-probiotic-awards.jpg` | 宝西兰详情新增部分_02.jpg | 790 × 1624 |
| `02-fish-oil-arctic.jpg` | 二折页-外星6-拷贝.jpg | 1200 × 1600 |
| `03-fish-oil-nutrition.png` | whc.png | 790 × 1333 |

网站文件保存在 `public/works/content-optimization/`，逐字节复制并核对 SHA-256，不裁切、不重新压缩、不修改来源文件。三张均为普通图片，复用营销主图的 `MediaCard` 与 `ProjectGrid`，电脑和手机均三张等高并排。使用 `equalImageHeight: true`，不设置统一 `frameRatio`；`src/project-layout.js` 按每张图片的宽高比生成比例列宽，保留各自真实比例，图片直接填满各列，没有外框或两侧填充留白。三张图片之间保留 6–14px 窄间隔，第一张较窄、第二张最宽、第三张居中间宽度，顶底自然对齐。标题下方直接接网格，右侧「跳过本组」前往详情设计；点击图片打开共享查看器，缩放最多为原尺寸，关闭后回到原位置。电脑悬停放大效果自动沿用，其他栏目与 IP 交互不变。

## 首页设计

按提供顺序接入三张完整长图，锚点为 `#homepage`：

| 位置 ID | 原文件 | 网站文件（homepage/ 下） | 原始尺寸 |
| --- | --- | --- | --- |
| `homepage-01` | 图层 1.png | 01-double-eleven-medical.png | 1200 × 5846 |
| `homepage-02` | 38_01.jpg | 02-calcium-womens-day.jpg | 1200 × 6381 |
| `homepage-03` | 618京东.jpg | 03-calcium-jd-618.jpg | 1200 × 5967 |

原图按字节复制到 `public/works/homepage/`，不裁切、不拉伸，不修改来源文件。首页栏目在电脑与手机上均使用一行三列固定比例预览，不使用固定滚动展台；按住鼠标上下拖动图片阅读，触屏使用原生手指滑动。保留作品名称，其下方增加「点击查看大图」线框按钮，居中同排对齐，按钮独立打开长图查看器，图片本身不触发弹窗。手机端列间距仍为 10px。原跳过位置换成黑底白字的「点击图片拖动查看」及上下箭头，取消跳转功能；原紫色「按住图片，上下拖动」标签已移除。提示块与中文副标题同排居中，靠右排列，600 字重；电脑端为 16px 字号、至少 52px 高，移动端为 14px 字号、至少 44px 高，380px 以下为 12px。其他栏目、IP 与开屏动效不变。

三个首页预览在电脑和手机上均为 `aspect-ratio: 9 / 16`，高度随卡片宽度等比变化，不设置 400px 高度上限。图片按宽度等比显示；桌面使用 `overflow: hidden` 配合纵向拖动调整阅读位置，滚轮只滚动网页。粗指针触屏使用 `overflow-y: auto` 原生滚动，保留完整原图。下方线框按钮打开查看器，缩放最大为原尺寸；关闭后返回按钮并保留原卡片的阅读位置。

## 详情设计

沿用 `#product-detail` 锚点，将原预留栏目改名为「详情设计」，移动至首页设计之前。三张原文件均按字节复制到 `public/works/product-detail/`，未裁切、压缩或覆盖来源文件：

| 位置 ID | 原文件 | 网站文件 | 原始尺寸 |
| --- | --- | --- | --- |
| `product-detail-01` | 317详情.jpg | 01-317-oxygen.jpg | 790 × 22967 |
| `product-detail-02` | xq-篮球 拷贝.jpg | 02-calcium-basketball.jpg | 1440 × 31255 |
| `product-detail-03` | 夏青 拷贝.jpg | 03-calcium-maternity.jpg | 1440 × 36476 |

详情与首页共用 `LongPagePreviewCard`。详情的 `previewRatio` 为 `9:20`，首页仍为 `9:16`；电脑与手机均三列，卡片按宽度等比显示原图，不拉伸图像。初始显示顶部，按住鼠标上下拖动可阅读至底部，图片本身不打开弹窗。每张图片下方、作品名称之后都有「点击查看大图」线框按钮，单独打开该作品的长图查看器。标题、右侧黑底白字拖动提示、底部作品名称、线框按钮及键盘阅读均与首页设计一致，两栏均不再提供跳过功能。

## 长图与其他栏目

普通作品的图片查看器采用窄边栏：顶部约 45px，只显示 13–14px 图片名称及关闭按钮；底部约 44px，放大镜图标旁的滑块可左右拖动，从适应尺寸的 50% 缩放至原图大小，并显示当前百分比，达到上限时显示「原尺寸」。默认 100% 适应窗口，小图不会自动放大；上限根据原图像素和窗口尺寸动态计算，窗口变大时会收紧上限，始终不超过原尺寸。放大后支持鼠标拖动及触屏平移；滑块方向键只控制缩放，不切换图片。其他焦点位置在图片无需平移时仍支持左右方向键切换作品，新作品重置缩放。分类标签、原尺寸按钮、计数和翻页按钮均移除；Esc、关闭、遮罩关闭以及焦点恢复保留。视频继续使用原生播放控件，不显示图片缩放滑块。

详情与首页的黑色拖动提示增加一次性的下落回弹入场：落点为右侧图片卡片上沿，接触后轻压、弹起，再回到原位置。只移动提示，不移动卡片；电脑和手机均生效。会等待开屏动画结束与图片加载完成，首次进入视野播放一次，刷新页面后可重新观看。拖动图片或使用键盘时立即结束回弹，窗口变化、离开视野、切后台或开启减少动态效果也会归位，跳转功能仍保持关闭。动效参数位于 `src/motion/hintBounce.js`。

首页和详情的原始长图保持完整，两栏均已接入本人提供的真实长图。按住鼠标上下拖动即可在预览框内浏览，松手停止，移开鼠标保留阅读位置；滚轮始终滚动网页，不控制图片。图片点击不打开弹窗，下方查看按钮已移除。触屏可以在图片内上下滑动。Tab 聚焦预览框后支持上下方向键、PageUp、PageDown、Home、End 阅读。预览比例与三列排版不变，其他栏目的媒体查看器和 IP 交互不变。

修改栏目数量、顺序、标题和默认比例：`src/gallery-data.js`。新增位置时，在 `src/work-media.js` 补上对应的编号即可。

## 页尾参考的使用

整站采用用户所贴参考中的浅薰衣草、淡粉灰、黑色粗体与留白风格。字体为 Epilogue Black / DM Sans；首屏与页尾复用参考提供的角色视频及校准后的视线映射。内容适配为蒋涛个人作品集，保留个人导航和联系入口，没有照搬参考里的陌生品牌标志或社交账户。主题覆盖文件是 `src/theme-lavender.css`。

视频来源为参考提供的完整 CloudFront 地址，原片存于 `public/footer-background.mp4`，全关键帧播放版为 `public/footer-scrub.mp4`。桌面以鼠标方位选择视频帧；手机将视频置于文字下方正常循环播放，并遵守减少动态效果设置。页尾不在视野内、或页面切到后台时暂停解码 / 播放。

网站仍为本地预览，未部署。头像及联系方式仍在 `src/content.js` 的 `profile` 中填写。
